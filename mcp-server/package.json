{
  "name": "browser-diag",
  "version": "3.2.0",
  "description": "Browser diagnostics and automation tools for MCP, CLI, and authenticated HTTP use.",
  "main": "server.js",
  "bin": {
    "browser-diag": "./server.js"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --test",
    "test": "node --test"
  },
  "keywords": [
    "mcp",
    "browser",
    "diagnostics",
    "playwright"
  ],
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "playwright-core": "^1.62.1",
    "zod": "^4.4.3"
  }
}
